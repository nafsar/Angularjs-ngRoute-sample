# Angularjs-ngRoute-sample
- anglarjs $route is used for linking URLs to controllers and views.
- It watches $location.url and tries to map the path to an existing route definition.
- http://uiux.biz/ngRoute/
